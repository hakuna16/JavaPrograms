package com.rituj.programs.opps;

/**
 * While isA implements inheritance, this implements composition and
 * aggregation.
 * 
 * This will be mostly done my using new operator.
 * <code> ANewClass a = new ANewClass()</code> But only the implementation will
 * be different.
 * 
 * In aggregation objects are weekly associated
 * 
 * In aggregation container object holds just the reference of the contained
 * object.
 * 
 * @author ritujkumar
 *
 */
public class HasAggregation {

	/**
	 * Aggregation : means mixer of different non-essential things. So, if without
	 * the container object if there is chance of contained object then it called as
	 * composition.
	 * 
	 * Example: </br>
	 * Without the department there is chance of having professors.
	 * 
	 * @param args
	 */
	public static void main(String[] args) {
		System.out.println();
	}
}
