package com.rituj.programs.opps;

/**
 * While isA implements inheritance, this implements compostion and aggregation.
 * 
 * This will be mostly done my using new operator.
 * <code> ANewClass a = new ANewClass()</code> But only the implementation will
 * be different.
 * 
 * In composition objects are strongly associated.
 * 
 * In composition container object holds directly the contained object
 * 
 * @author ritujkumar
 *
 */
public class HasAComposition {

	/**
	 * Composition : means mixer of different essential things. So, if without the
	 * container object if there is no chance of contained object then it called as
	 * composition.
	 * 
	 * Example: </br>
	 * Without the university there is no chance of having professors.
	 * 
	 * @param args
	 */
	public static void main(String[] args) {

	}
}
